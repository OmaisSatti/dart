// Arithmetic: +, -, *, /, ~/ (truncating), % (modulus), ++, --.
// Equality/Relational: ==, !=, >, <, >=, <=.
// Type Test: is, is!, and as for casting.
// Assignment: =, +=, -=, etc., including ??= for null-aware assignment.
// Logical: && (AND), || (OR), ! (NOT).
// Bitwise/Shift: &, |, ^, ~, <<, >>, >>>.
// Conditional/Ternary: condition ? expr1 : expr2 and expr1 ?? expr2 (if null).
// Cascade: .. and ?.. for operating on the same object.
// Null-aware: ?. (access), ! (assertion), and ...? (spread). 